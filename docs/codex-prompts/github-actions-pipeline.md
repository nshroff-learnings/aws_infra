# Create Terraform GitHub Actions Pipeline

Use `$terraform-aws-infra`.

Create a secure Terraform deployment workflow with separate fmt, validate, scan, plan, and gated apply jobs. Use GitHub OIDC for AWS authentication, least permissions, protected GitHub Environments, stack-level concurrency, safe artifacts, and no static AWS keys.

Document required repository variables, environment names, and AWS IAM role trust assumptions.
