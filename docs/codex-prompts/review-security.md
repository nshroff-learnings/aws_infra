# Review Terraform Security

Use `$terraform-aws-infra`.

Review Terraform and GitHub Actions for AWS security issues: secrets, IAM wildcards, trust policies, public access, ingress, encryption, logging, retention, state backend safety, OIDC, workflow permissions, approval gates, and unsafe artifacts.

Respond with findings first, ordered by severity, with concrete fixes.
