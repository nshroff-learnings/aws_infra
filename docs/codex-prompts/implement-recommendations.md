# Implement Review Recommendations

Use `$terraform-aws-infra`.

Implement only the approved recommendations supplied by the user. Preserve unrelated changes, keep patches small, run `terraform fmt -recursive`, and validate changed root modules when dependencies are available.

Report implemented items, skipped items, assumptions, and validation results.
